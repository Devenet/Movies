<!doctype html>
<html>
<head>
  {include="page.head"}
</head>
<body>
  {include="page.header"}
    
  <main class="container">
    <h1>Welcome to Movies</h1>
    <p class="lead">It’s the first time you run <b>{#MOVIES#}</b>.<br class="d-block d-sm-none"> Let’s configure it.</p>
      
    {if="!empty($error_img)"}
    <div class="alert alert-warning">
      <strong>Damn!</strong> It seems that the PHP library GD is disabled or not installed on your server (the function <code>imagecreatefromjpeg()</code> is undefined).
      <br>You can still use the application but you will not be able to import images from URL and generate thumbnails until you activate it.
    </div>
    {/if}
    
    <div class="row mt-4">
      <form class="col-lg-6" method="post">
        <fieldset>
          <legend>Authentification</legend>

          <div class="mb-3">
            <label for="login" class="form-label">Username</label>
            <input type="text" class="form-control" id="login" name="login" placeholder="Nicolas" autofocus required>
          </div>
          <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <input type="password" class="form-control" id="password" name="password" placeholder="Secret" required>
          </div>
        </fieldset>

        <fieldset>
          <legend>Settings</legend>
          
          <div class="mb-3">
            <label for="title" class="form-label">Title</label>
            <input type="text" class="form-control" id="title" name="title" placeholder="{#MOVIES#}" value="{#MOVIES#}" aria-describedby="h_title">
            <div id="h_title" class="form-text">You can edit it later <i class="bi bi-emoji-smile"></i>.</div>
          </div>
          
          <div class="mb-3">
            <label for="author" class="form-label">Author</label>
            <input type="text" class="form-control" id="author" name="author" placeholder="Nicolas Devenet" aria-describedby="h_author">
            <div id="h_author" class="form-text">
              Used for copyright notice and for social sharing (when you post the link of a movie page).
              <br>If not set, your username will be used.
            </div>
          </div>
        </fieldset>

        <button type="submit" class="mt-3 btn btn-primary">Install</button>
      </form>
    </div>
  </main>
      
  {include="page.footer"}
</body>
</html>